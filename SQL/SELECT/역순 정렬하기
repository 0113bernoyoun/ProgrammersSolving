SELECT NAME, DATETIME FROM ANIMAL_INS ORDER BY ANIMAL_ID DESC



NAME과 DATETIME의 값을 필요로 하므로
SELECT문에는 NAME, DATETIME 을 넣는다.

TABLE은 ANIMAL_INS에서 조회한다.

ANIMAL_ID 값을 기준으로 내림차순 정렬을 하기 위해 ORDER BY ANIMAL_ID 뒤에 DESC를 붙인다.